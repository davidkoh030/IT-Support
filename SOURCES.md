# Sources

Verified this session unless marked otherwise. "Informs" states exactly which document/decision the source supports - none of these are cited as proof of an industry standard or universal practice, per the BUILD PROMPT's own framing.

| Source | Accessed | What it supports | Informs |
| --- | --- | --- | --- |
| [ShaneCunn/Support-Ticket-system](https://github.com/ShaneCunn/Support-Ticket-system) @ commit `c8c469b8388ab585e395548481d5fc24ae3f4475` (17 Oct 2018) | This session (`git clone`, read-only structural inspection) | Legacy stack (Laravel 5.5.25/PHP 7), route/schema/seeder/role/upload structure, and the licence facts below. | `TECH_STACK.md` §1, `MIGRATION_PLAN.md` §2-3. |
| Root `composer.json` (`"license": "proprietary"`, author Shane Cunningham) and `support/composer.json` (`"license": "MIT"`, `"name": "laravel/laravel"`) in the same repository | This session | Confirms the two manifests disagree, and that the MIT one is Laravel's own stock skeleton file, not a statement about the application code. Repo-wide search confirmed no `LICENSE`/`LICENCE`/`COPYING` file exists anywhere except the bundled `support/public/js/tinymce/license.txt` (TinyMCE's own LGPL 2.1 text for that vendored asset). | The decision in `MIGRATION_PLAN.md` §1 to rebuild independently rather than reuse any legacy code. |
| Laravel News: ["Laravel 13 Released"](https://laravel-news.com/laravel-13-released) | This session (web search) | Laravel 13 requires PHP 8.3+ and supports PHP 8.3-8.5. | `TECH_STACK.md` §3 (PHP/Laravel version pairing). |
| [benjamincrozat.com/laravel-versions](https://benjamincrozat.com/laravel-versions) | This session (web search) | Laravel 13 released ~17 March 2026; Laravel's standard support cycle is 18 months of bug fixes and 2 years of security fixes from a major version's release. | `TECH_STACK.md` §3 (why Laravel 13 rather than an older LTS was chosen: it was the version actually resolved by `composer create-project laravel/laravel` with no pin at implementation time, and it remains security-supported well beyond this pilot's horizon). |
| `laravel.com/docs/13.x/releases` | **Not directly fetched** - the environment's network egress proxy blocked `laravel.com` and `endoflife.date` directly (`EGRESS_BLOCKED` errors from both `WebFetch` calls attempted this session) | The above two secondary sources were used instead. If an authoritative primary-source check is needed before relying on this further, re-fetch `laravel.com/docs/13.x/releases` from an environment without that block. | Flagged here rather than silently citing a page that was never actually opened. |
| [Atlassian: impact/urgency priority matrix](https://support.atlassian.com/jira-service-management-cloud/docs/how-do-i-create-a-matrix-using-impact-and-urgency-values/) | Carried forward from the BUILD PROMPT's own research table (not re-fetched this session) | Priority can be derived from impact x urgency with a customisable matrix - a pattern, not a specific set of values. | `priority_matrix_rules` seed data and `PriorityCalculator` design (`REQUIREMENTS.md` §7) - the specific 3x3 mapping and the P1-P4 minute targets are this project's own demo design, not quoted from Atlassian. |
| [Atlassian: SLA calendars](https://support.atlassian.com/jira-service-management-cloud/docs/set-up-sla-calendars/) | Carried forward, not re-fetched | Service clocks can use working days/times/timezone/holidays as a distinct concept from general business hours. | `Calendar` model design (`ARCHITECTURE.md` §4) - separating SLA support-hours calendars from `sites.operating_hours_*`. |
| [UK BIM Framework information protocol, edition 3](https://www.ukbimframework.org/wp-content/uploads/2021/02/Protocols_Ed3.pdf) | Carried forward, not re-fetched | Information-management responsibilities, CDE security/preservation, and access framed around project appointments - guidance, not a ticketing product spec. | The "information manager" field on `projects` and the external-access approval/expiry model (`REQUIREMENTS.md` §8). **This project makes no ISO 19650 compliance claim** - the field exists because the prompt asked for it, not because this pilot has been assessed against that standard. |
| [Microsoft identity platform: OIDC](https://learn.microsoft.com/en-us/entra/identity-platform/v2-protocols-oidc) and [authorisation-code flow](https://learn.microsoft.com/en-us/entra/identity-platform/v2-oauth2-auth-code-flow) | Carried forward, not re-fetched (and not yet implemented - Phase C item 1) | Documents the OIDC sign-in and auth-code flow this pilot's *planned* Entra integration would use. | `ROADMAP.md` [1] acceptance criteria (tenant/object-ID identity, not email; authorization-code flow only). |
| [Singapore PDPC: data protection obligations](https://www.pdpc.gov.sg/data-protection-obligations) | Carried forward, not re-fetched | Lists protection, retention-limitation, and transfer-limitation among PDPA's obligations (obligations summary only, not the full Act). | `REQUIREMENTS.md` §10's framing of PDPA as a deployment-review *input*, not a guarantee of compliance - this pilot makes no hosting-region or compliance claim of its own. |
| PHP runtime introspection (`php -v`, `php -m`) in this session's container | This session | PHP 8.4.19, available extensions (`pdo_sqlite`, `pdo_mysql`, `pdo_pgsql`, no `mysqli` CLI binary, no `sqlite3` CLI binary, no `mysqld`), Composer 2.8.12, Node 22/npm 10. | `TECH_STACK.md` §4 (why SQLite was used for the local runtime instead of MySQL - a tooling constraint of this specific container, not a recommendation for a real deployment). |
| `docker ps` in this session's container (`failed to connect to the docker API ... no such file or directory`) | This session | No Docker daemon available in this container. | `LOCAL_SETUP.md` Path B and `docker-compose.yml`/`docker/php.Dockerfile` are marked "reviewed for correctness, not executed" rather than verified. |

## What was *not* independently re-verified this session

The Atlassian, UK BIM Framework, Microsoft, and PDPC sources above were inherited from the BUILD PROMPT's own "Research basis and interpretation" table and were **not re-fetched or re-checked** in this session - they are listed here for traceability of which design decisions they originally informed, not as newly-confirmed facts. Anyone relying on this document for a compliance decision should treat those four rows as pointers to re-verify, not as sources this session independently confirmed still say what they said when the original prompt was written.
